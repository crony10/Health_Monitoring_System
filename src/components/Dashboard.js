import React, { Fragment } from "react";
// import { toast } from "react-toastify";

const Dashboard = () => {
  return (
    <Fragment>
      <h1>Dashboard</h1>
    </Fragment>
  );
};

export default Dashboard;
